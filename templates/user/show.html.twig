{% extends "base-admin.html.twig" %}
{% block body %}
    <style>


    </style>
    <h1>User</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Firstname</th>
            <td>{{ user.firstname }}</td>
        </tr>

            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>


            <tr>
                <th>Lastname</th>
                <td>{{ user.lastname }}</td>
            </tr>

            <tr>
                <th>Resetcode</th>
                <td>{{ user.resetcode }}</td>
            </tr>
        </tbody>
      </table>

    <a href="{{ path('app_user_index') }}"><button type="submit" class="btn btn-success">back to list</button></a>

    <a href="{{ path('app_user_edit', {'id': user.id}) }}">


    </a>


    {{ include('user/_delete_form.html.twig') }}

{% endblock %}
