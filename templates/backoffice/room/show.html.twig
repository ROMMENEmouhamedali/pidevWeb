{% extends "base-admin.html.twig" %}

{% block title %}Room{% endblock %}

{% block body %}
    <h1>Room</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Roomid</th>
                <td>{{ room.roomid }}</td>
            </tr>
            <tr>
                <th>Roomnumber</th>
                <td>{{ room.roomnumber }}</td>
            </tr>
            <tr>
                <th>Roomtype</th>
                <td>{{ room.roomtype }}</td>
            </tr>
            <tr>
                <th>Roompricepernight</th>
                <td>{{ room.roompricepernight }}</td>
            </tr>
            <tr>
                <th>Roomdirection</th>
                <td>{{ room.roomdirection }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_room_index') }}">back to list</a>

    <a href="{{ path('app_room_edit', {'roomid': room.roomid}) }}">edit</a>

    {{ include('backoffice/room/_delete_form.html.twig') }}
{% endblock %}
