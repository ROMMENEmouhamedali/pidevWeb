/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
/*package com.happ.controller;

import com.happ.dao.AssistantChefDao;
import happ.entity.AssistantChef;
import java.net.URL;
import java.util.ResourceBundle;
import javafx.fxml.FXML;
import javafx.fxml.Initializable;
import javafx.scene.control.Alert;
import javafx.scene.control.Button;
import javafx.scene.control.TextField;*/

/**
 *
 * @author ASUS
 */
/*public class AddAssistantChefController implements Initializable {

  @FXML
    private Button btn;
    @FXML
    private TextField nom;
    @FXML
    private TextField prenom;*/

    /**
     * Initializes the controller class.
     *
     * @param url
     * @param rb
     */

 /*  @Override
    public void initialize(URL url, ResourceBundle rb) {
        // TODO     
        btn.setOnAction(event -> {
            
            AssistantChef ac = new AssistantChef(nom.getText(), prenom.getText());
            AssistantChefDao pdao = AssistantChefDao.getInstance();
            pdao.insert(ac);
        
        Alert alert = new Alert(Alert.AlertType.INFORMATION);
        alert.setTitle("Information Dialog");
        alert.setHeaderText(null);
        alert.setContentText("Personne insérée avec succés!");
        alert.show();
        nom.setText("");
        prenom.setText("");
        });
        
        
    }
}
*/